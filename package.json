{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.34.1"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": "14.x",
    "yarn": ">=1.15"
  },
  "scripts": {
    "start": "yarn redwood dev",
    "db:migrate": "yarn redwood prisma migrate dev"
  }
}
